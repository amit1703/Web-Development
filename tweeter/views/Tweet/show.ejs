<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <ul>
      <% for(let i =0 ; i<tweets.length;i++){ %>
      <li>
        <b><%= tweets[i].body  %>- </b><a href="/<%= tweets[i].user._id %>"><%= tweets[i].user.name %></a></li>

      <% } %>
    </ul>
  </body>
</html>
